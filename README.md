instructions for deploy test utils:

1) cwd => /home/username/
2) pre-install the package: sudo apt install python3-venv -y
3) git clone https://gitlab.dolomant.local/gashimov/autotest
4) python3 -m venv autotest/ && cd autotest && source bin/activate
5) ./setup.py
